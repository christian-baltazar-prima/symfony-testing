services:
  app:
    build: .
    container_name: symfony-testing
    restart: unless-stopped
    volumes:
      - ./:/var/www/html
    command: symfony server:start --port=8000  --no-tls --allow-http --allow-all-ip --dir=public
